# Must include /api (or the app will append it).
# Local:
VITE_API_URL=http://localhost:5000/api

# Production (Vercel env var — set at build time):
# VITE_API_URL=https://ecommerceweb-7p1x.onrender.com/api
